# UI Web Demo — Bloret Launcher 展示

这是一个演示项目，用于把原生 PyQt UI (`ui/` 文件夹) 用网页形式还原，风格参考 QFluentWidgets 的 FluentWindow。

快速开始：

```bash
cd ui-web-demo
npm install
npm run dev
```

说明：
- UI 使用 `@fluentui/web-components` 做控件样式（已在入口文件导入）。
- 目前为静态 UI 展示，功能行为未实现，只还原外观与布局。

后续我会把更多 `.ui` 文件转换为页面并完善样式。 
